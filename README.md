# Daily Reporter

업무 시간을 기록하고 일일/주간 보고서를 자동 생성하는 Windows 데스크탑 앱입니다.

---

## 기능

- **출근/퇴근 기록** — 클릭 한 번으로 타임스탬프와 함께 로그 파일에 저장
- **실시간 시계** — 현재 시각을 메인 화면에 표시
- **주기 알림** — 설정한 주기(기본 60분)마다 알림음과 함께 업무 내용 입력 팝업 표시 (`Enter`로 제출)
- **마지막 기록 표시** — 가장 최근 기록을 메인 화면에 표시, 파일 외부 수정 시 자동 반영
- **복사 버튼** — 마지막 기록의 업무내용을 클립보드에 복사
- **로그 확인** — 오늘 로그 파일을 기본 앱으로 바로 열기
- **일일보고 생성** — 내장 템플릿 기반으로 당일 로그를 채운 hwpx 파일 자동 생성
- **주간보고 생성** — 내장 템플릿 기반으로 기간 내 로그를 집계한 hwpx 파일 자동 생성
- **출근 상태 자동 복원** — 앱 재시작 시 오늘 로그에 출근 기록이 있으면 알람 자동 재개
- **자동 시작** — 윈도우 부팅 시 자동 실행 설정/해제 (설정 메뉴)

**설정 메뉴 항목:**

| 항목 | 설명 |
|------|------|
| 윈도우 시작 시 자동 실행 | 부팅 시 앱 자동 실행 (레지스트리 등록) |
| 이전 내용 자동 입력 | 팝업 열릴 때 직전 PROJECT명·업무내용 자동 채움 |
| 시작 시 출근 상태 자동 복원 | 재시작 시 오늘 로그에 출근 기록이 있으면 알람 재개 |
| 알림 주기 설정... | 알림 간격을 분 단위로 설정 (기본 60분) |
| 보고자 성명 설정... | 보고서에 삽입될 이름 (기본 홍길동) |

---

## 다운로드 및 실행

1. [dist/daily_reporter.exe](dist/daily_reporter.exe) 다운로드 후 원하는 폴더에 저장 (단일 파일, 별도 설치 불필요)
2. `daily_reporter.exe` 실행
3. 최초 실행 시 **설정 → 보고자 성명 설정...** 에서 이름 입력
4. **출근** 버튼 클릭 → 출근 시각 기록 + 정시 알림 시작
5. 매 정시마다 팝업이 뜨면 PROJECT명·업무내용 입력 후 **기록** 또는 `Enter`
6. **퇴근** 버튼 클릭 → 퇴근 시각 기록 + 알림 종료
7. **일일보고 생성** 버튼 → 당일 보고서 hwpx 파일 자동 생성 및 열기

> 로그 파일과 생성된 보고서는 `daily_reporter.exe`가 위치한 폴더에 저장됩니다.
> 템플릿 파일은 EXE에 내장되어 있어 별도 파일이 필요 없습니다.

---

## 로그 파일 형식

날짜별로 `YYYY-MM-DD.csv` 파일로 저장됩니다. 4개 열(date, time, project, description).

```
2026-05-12,09:03:52,출근,
2026-05-12,10:00:00,피플카운터,ESP32 BLE 스캔 최적화
2026-05-12,11:00:00,"피플카운터, 집합수요",파라미터 튜닝
2026-05-12,18:07:31,퇴근 (자동),
```

프로젝트명에 쉼표가 포함되면 자동으로 큰따옴표로 감싸집니다.

---

## 템플릿 사용 방법

템플릿 파일은 EXE에 내장되어 있으므로 별도 파일 관리가 필요 없습니다.  
보고자 성명은 **설정 → 보고자 성명 설정...** 에서 입력하면 보고서에 자동 반영됩니다.

### 일일보고

보고서 생성 시 다음 항목은 **로그 파일에서 자동으로 채워집니다**.

| 항목 | 인식 방법 | 소스 |
|------|-----------|------|
| 날짜 | `YYYY년 MM월 DD일` 패턴 (첫 번째 등장) | 로그 파일의 날짜 열 |
| 출근 시각 | `HH:MM:SS` 패턴 (XML 기준 첫 번째 등장) | 로그의 `출근` 행 |
| 퇴근 시각 | `HH:MM:SS` 패턴 (XML 기준 두 번째 등장) | 로그의 `퇴근` 행 |
| 근무시간 | `N시간` 패턴 (첫 번째 등장) | 출퇴근 차이 자동 계산 |
| 보고자 성명 | 설정값 | 설정 메뉴의 보고자 성명 (미설정 시 `홍길동`) |
| 시간 슬롯 | 아래 슬롯 문자열과 정확히 일치하는 텍스트 | 로그 기록 순서대로 실제 구간으로 교체 |
| PROJECT명 | 각 슬롯 행의 빈 텍스트 칸 (첫 번째) | 팝업 입력의 PROJECT명 |
| 업무내용 | 각 슬롯 행의 빈 텍스트 칸 (두 번째) | 팝업 입력의 업무내용 |

**시간 슬롯 — 템플릿에 이 텍스트가 정확히 있어야 합니다:**

```
09:00~10:00  10:00~11:00  11:00~12:00
12:00~13:00  13:00~14:00  14:00~15:00
15:00~16:00  16:00~17:00  17:00~18:00
```

> 슬롯 순서대로 로그 항목이 배정됩니다. 로그가 4개면 앞 4개 슬롯만 채워지고, 나머지 슬롯은 시간 레이블 포함 완전히 비워집니다.

**템플릿 수정 시 주의사항:**
- 날짜·출퇴근시각·근무시간 셀은 값이 무엇이든 패턴으로 인식하므로 자유롭게 수정 가능
- PROJECT명·업무내용 칸은 **비워두어야** 자동 삽입됨 (기존 텍스트가 있으면 삽입 안 됨)
- 시간 슬롯 텍스트(`09:00~10:00` 등)는 코드와 정확히 일치해야 하며, 변경 시 `_templates.py`의 `_TMPL_TIMES` 목록도 함께 수정 필요

---

### 주간보고

| 항목 | 인식 방법 | 소스 |
|------|-----------|------|
| 날짜 | `MM.DD` 패턴 (첫 번째 등장) | 선택한 기간의 종료일(금요일) |
| PROJECT명 | 템플릿 행의 기존 텍스트를 동적 추출 후 치환 | 기간 내 로그의 PROJECT명 |
| 업무내용 | 템플릿 행의 기존 텍스트를 동적 추출 후 치환 | 기간 내 로그의 업무내용 집계 |

**동작 방식:**
- 지정 기간의 모든 `YYYY-MM-DD.csv`를 읽어 PROJECT별로 업무내용을 집계
- 복합 프로젝트(`"피플카운터, 집합수요"`)는 쉼표 기준으로 분리하여 각각 집계
- 최대 6행까지 채움 (템플릿 행 수 기준)
- 템플릿의 기존 텍스트(전주 데이터)를 읽어서 같은 위치에 새 데이터로 교체하므로, **전주 데이터가 채워진 상태**로 유지하면 됨

---

## 빌드

```bash
pip install pyinstaller
pyinstaller daily_reporter.spec
```

빌드 결과물은 `dist/daily_reporter.exe`에 생성됩니다.  
템플릿은 `_templates.py`에 base64로 내장되어 있으며, 빌드 시 별도 파일 없이 EXE 단독으로 배포 가능합니다.

---

## 개발 환경

- Python 3.13
- tkinter (표준 라이브러리)
- PyInstaller 6.x

---

## 기여 방법

1. 이 레포를 **Fork**
2. 기능 브랜치 생성 (`git checkout -b feat/기능명`)
3. 변경 후 커밋 — [Conventional Commits](https://www.conventionalcommits.org/) 형식 권장
   ```
   feat: 새 기능 설명
   fix: 버그 수정 설명
   docs: 문서 변경
   ```
4. Fork한 레포에 push (`git push origin feat/기능명`)
5. 원본 레포로 **Pull Request** 생성

> 템플릿을 수정하고 싶다면 `_templates.py`의 base64를 직접 편집하기보다,  
> hwpx 파일을 수정 후 `create_worklog.py` 등을 활용해 재인코딩하는 것을 권장합니다.

---

@ 2026 Made by YKJ
